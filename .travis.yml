language: python
python:
  - "3.6"  
install:
  - pip install -r requirements.txt
script:
  - python -c "import sys; print(sys.version, '\n', sys.prefix)";
  - pytest

after_success:
  - echo == ALL TESTS PASSED ==
  - coveralls

after_failure:
  - echo == FAILURE ==
